<!--components/frontCard/music/music.wxml-->
<view class="container">
    <image class="poster_img" src="{{image}}" />
    <image class="play_img" catch:tap="onPlay"
    src="{{!playing ? pauseSrc : playSrc }}" />
</view>

